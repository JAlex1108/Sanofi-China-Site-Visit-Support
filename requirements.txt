# Direct runtime dependencies for the failure-mode detection pipelines.
# Pinned to the versions verified working on Windows 11 + Python 3.12.
# Transitive deps (botocore, scipy, pillow, ...) resolve automatically via pip.
# See requirements-lock.txt for the full frozen set.
#
# PyTorch is pulled from the CUDA 12.8 wheel index so GPUs (including Blackwell
# / RTX 50xx) are used for YOLO inference. The default PyPI build is CPU-only
# and will cause ultralytics to run on the CPU at ~10 fps even with a GPU
# present. To force a CPU-only install, drop the --extra-index-url line and
# replace the torch/torchvision pins with the +cpu variants.
--extra-index-url https://download.pytorch.org/whl/cu128

boto3==1.42.93
matplotlib==3.10.8
numpy==2.4.4
opencv-python==4.13.0.92
openpyxl==3.1.5
plotly==6.7.0
python-dotenv==1.0.1
torch==2.12.0
torchvision==0.27.0
ultralytics==8.4.50
